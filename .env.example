# --- API ---
USE_POSTGRES=1
DATABASE_URL=postgresql://postgres:postgres@db:5432/aether

# Registry JSON path inside container
REGISTRY_PATH=/app/wmag_kernel/registry/registry_support_v1.json

# Gemini (optional)
# GEMINI_API_KEY=xxx
GEMINI_MODEL=gemini-3-flash-preview

# MCP servers (optional)
# MCP_SERVERS=[{"id":"calendar","base_url":"http://host.docker.internal:9100","timeout_s":15}]

# Cockpit config dir
CONFIG_DIR=/app/config
TENANT_CONFIG_DIR=/app/config/tenants
TENANT_OVERRIDES_DIR=/app/config/tenants

JWT_SECRET=dev_jwt_secret_change_me
JWT_ACCESS_TTL_S=900
JWT_REFRESH_TTL_S=1209600
REGISTRY_LAYERS_DIR=/app/config